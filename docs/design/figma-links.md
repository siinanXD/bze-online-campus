# Figma-Verweise — bze-online-campus

Noch keine Datei verknuepft.

## Was hier hingehoert

| Datei | Zweck | Link |
|---|---|---|
| _(noch keine)_ | | |

## Was hier nicht hingehoert

**Keine Tokens, keine Zugangsdaten, kein persoenlicher Zugriffsschluessel.**
Nur Links auf Figma-Dateien und -Frames. Ein Figma-Token in einer
Markdown-Datei ist ein veroeffentlichter Token, sobald das Repository geteilt
wird — und dieses hier ist teilweise oeffentlich.

## Wie ein Verweis aussieht

```
| Startseite | Entwurf der Startseite | https://figma.com/design/<datei-id>/...?node-id=<frame> |
```

Der `node-id`-Teil ist wichtig: ohne ihn landet der Leser in der Datei, aber
nicht am gemeinten Frame.

## Anbindung

Der lokale Figma-MCP-Server ist **nicht eingerichtet**. Die Figma-Desktop-App
ist auf diesem Rechner nicht installiert (geprueft am 2026-08-03).

Anleitung und Beispielkonfigurationen:
`C:\Dev\AI-Workspace\docs\FIGMA-INTEGRATION.md`

Solange das so ist, laeuft die Uebergabe ueber den Ersatzweg: Screenshots,
SVG-Export, Komponentenliste und die extrahierten Tokens. Das ist kein
Notbehelf — fuer die meisten Aufgaben reicht es aus.
